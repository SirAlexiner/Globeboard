version: '3.8'

services:
  globeboard:
    build: .
    ports:
      - '${PORT}:${PORT}'
    environment:
      - PORT=${PORT}
      - TOKEN=${TOKEN}
    volumes:
      - ./.secrets:/root/.secrets:ro
